server:
  port: 8080
  servlet:
    context-path: /
    encoding: 
      charset: UTF-8
      enabled: true
      force: true
      
spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/blog?serverTimezone=Asia/Seoul&useSSL=false&characterEncoding=utf-8
    username: shim
    password: gusdn1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # 없으면 만들고 있으면 그대로 사용
    show-sql: true # mysql debug mode, 실제 쿼리가 로그로 보임
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  devtools:
    livereload:
      port: 35730


  jackson:
    serialization:
      fail-on-empty-beans: false
      